# Clients Sync

## Purpose
Shared handoff for client and demo apps under `clients/`.

## Projects
- `terracomm/` = active client app, confirm before changing
- `septic-dispatch/` = generic septic demo, no live client
- `mcmahon-ranch/` = ranch management app
- `Take Pride/` = empty

## Recent Cleanup
- Renamed the septic demo directory to `septic-dispatch/`.
- Added `mcmahon-ranch/` to the documented project map.

## Working Rule
- Update each project's own `SYNC.md` after meaningful work.

## Next Recommended Step
- Add project-level docs as soon as a new client folder gets its first real deliverable.
