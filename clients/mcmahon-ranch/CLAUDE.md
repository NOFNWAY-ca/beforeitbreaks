# CLAUDE.md — McMahon Ranch

Single-file ranch management app for a client-style workflow.

## Files

- `mcmahon-ranch.html` — current app

## Constraints

- Single HTML file only.
- No framework, no build step, no external JS dependencies.
- Data stays local in the browser.

## Agent Sync

- Read `SYNC.md` before starting work.
- Update `SYNC.md` after meaningful changes so Codex and Claude share current state.
